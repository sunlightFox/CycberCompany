{
  "case_id": "FNEW50R10-032",
  "category": "操作系统",
  "title": "查代理",
  "peer_ref": "oc_round10_system",
  "prompt": "接口不通，先给只读排查代理、DNS、证书的思路，不要直接改。",
  "verdict": "pass",
  "score": 100,
  "notes": [],
  "reply_text": "先给结论：先不要改配置，只做“观察型”排查。按顺序看三类：**请求是否走了代理 → 域名是否解析对→ TLS证书链是否可信/匹配**。我来理一下只读排查思路。\n##1.先确认失败类型只读执行：```bashcurl-vI --connect-timeout10https://目标域名/路径```重点看：-是否卡在`Trying...`-是否出现 `Connectedto ...`-是否有 `CONNECTproxy`- TLS阶段是否报错：-`certificate verify failed`-`SSL: noalternative certificatesubjectname`-`unable toget localissuercertificate`\n- HTTP 状态码：-`407`多半是代理认证-`502/504`可能是代理或上游-`403`可能是网关、WAF、鉴权或源站策略\n##2.只读排查代理###看当前环境变量Linux/macOS：```bashenv|grep-iproxy```WindowsPowerShell：```powershellGet-ChildItemEnv:*proxy*```\n\n关注：\n\n```textHTTP_PROXYHTTPS_PROXYALL_PROXYNO_PROXY```\n\n判断点：-目标域名是否应该走代理？- `NO_PROXY` 是否包含目标域名、内网域名、IP段？-`HTTPS_PROXY` 是否配置到了不可达代理？-代理是否要求认证但当前没有凭据？###看curl 实际是否走代理```bashcurl -vI https://目标域名/路径```如果看到类似：\n\n```text*Usesproxy envvariableHTTPS_PROXY*EstablishHTTPproxytunnelto目标域名:443* CONNECT目标域名:443HTTP/1.1```说明请求走了代理。###只做对比测试，不改全局配置本次命令临时绕过代理：```bashcurl -vI--noproxy'*' https://目标域名/路径```本次命令临时指定代理：```bashcurl-vI-xhttp://代理地址:端口https://目标域名/路径```判断：-走代理失败、绕过成功：代理侧问题概率高-绕过失败、走代理成功：直连网络/DNS/出口策略问题概率高-两边都失败：继续看DNS和证书##3.只读排查DNS###查本机解析结果Linux/macOS：```bashnslookup目标域名```或：```bashdig目标域名```Windows：```powershellResolve-DnsName目标域名```关注：-解析到的IP是否符合预期-是否解析到内网地址/错误环境地址-是否有多个A/AAAA记录-是否优先走了IPv6### 对比公共DNS```bashdig@8.8.8.8目标域名dig @1.1.1.1目标域名```国内环境也可以只读对比：```bashdig @223.5.5.5目标域名dig @114.114.114.114目标域名```判断：-本地 DNS和公共DNS结果不同：可能是内网 DNS、污染、分流、Split DNS-只有IPv6返回或 IPv6 不通：可能是IPv6 路由问题-CNAME指向异常：可能是环境、灰度、CDN配置问题###看当前 DNS 配置，只读Linux：```bashcat/etc/resolv.conf```\n\nmacOS：\n\n```bashscutil--dns```\n\nWindows：```powershellipconfig /all```##4.只读排查证书/TLS###查看证书链和握手```bashopenssl s_client-connect目标域名:443 -servername目标域名-showcerts</dev/null```重点看末尾：```textVerify return code:0 (ok)```\n\n如果不是`0`，常见问题：-`unable to getlocalissuer certificate`：本机不信任中间证书或代理替换证书- `certificatehasexpired`：证书过期- `Hostnamemismatch`/域名不匹配：证书SAN 不包含目标域名- `selfsigned certificate`：自签证书未被信任-握手失败：TLS版本、SNI、代理 MITM或网关问题###看证书主题和有效期```bashecho |openssl s_client-connect目标域名:443-servername目标域名2>/dev/null|opensslx509-noout-subject -issuer-dates```关注：\n\n```textsubject=issuer=notBefore=notAfter=```判断：-`subject`/SAN是否匹配目标域名-`issuer` 是否是预期 CA-是否被公司代理证书替换-是否过期或还未生效###用curl看TLS报错```bashcurl -Iv https://目标域名/路径```不要先加`-k`，因为`-k` 会跳过证书校验，容易掩盖问题。如果只是为了对比，可以临时跑一次：\n\n```bashcurl-Iv-khttps://目标域名/路径```判断：\n\n-不加`-k`失败，加`-k`成功：证书信任链/域名匹配问题概率高- 加 `-k`也失败：不是单纯证书校验问题，继续看网络、代理、TLS 协议##5. 快速判断表|现象 |优先怀疑||\n|\n||`407 Proxy AuthenticationRequired`|代理认证||`Couldnotresolvehost`|DNS||`Connectiontimedout`|网络、代理、出口、防火墙 ||`Connectionrefused`|目标端口未监听或被拒绝||`SSL certificateproblem` |证书链/本地信任|\n|`no alternativecertificatesubjectname` |证书域名不匹配 |\n|`CONNECTtunnel failed`|代理到目标不可达或被代理策略拦截||直连成功、走代理失败|代理问题 ||走代理成功、直连失败|直连网络/DNS/出口策略问题|\n| 本地 DNS 和公共DNS结果不同|DNS分流/污染/内外网解析差异|\n##6.建议你先收集这几段输出只读即可：```bashcurl -vI --connect-timeout10 https://目标域名/路径env | grep -i proxynslookup目标域名openssl s_client -connect目标域名:443 -servername目标域名 -showcerts </dev/null```\n\n注意脱敏：\n\n- token- cookie- Authorization- 内网代理账号密码-真实业务路径里的敏感参数先按这个顺序查，基本能把问题收敛到：**代理、DNS、证书，或上游服务本身**。",
  "turn_id": "turn_d9952b1f4da94a689b09f6458c4a5716",
  "conversation_id": "conv_98bee445eed444ed87729bbc7c2a892d",
  "trace_id": "trc_3938f49177cd4d988a2005bfc95addca",
  "route_brain_id": "brain_not_configured",
  "model_started": true,
  "model_completed": true,
  "usage_total_tokens": null,
  "delivery_sent": true,
  "event_types": [
    "turn.queued",
    "turn.queue_started",
    "content.normalized",
    "turn.started",
    "context.started",
    "context.ready",
    "intent.detected",
    "mode.selected",
    "route.selected",
    "model.started",
    "model.completed",
    "response.delta",
    "response.completed",
    "turn.completed"
  ],
  "route_type": null,
  "task_status": null
}