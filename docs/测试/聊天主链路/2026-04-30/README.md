# 聊天主链路重型压力测试

测试批次：`CHAT-E2E-20260430-POWER`

## 目标

本目录用于承载第 10 轮聊天主链路重型压力测试。测试覆盖闲聊、复杂聊天、知识总结、记忆、人格、上下文、任务、工具、Skill、MCP、资产、知识库、浏览器执行、恢复和安全回归。

本轮只新增测试文档、执行脚本和报告，不修改后端业务代码，不修复问题。

## 执行环境

- 使用当前仓库 `data` 环境，不使用临时数据库。
- 测试对象固定为 `mem_xiaoyao` 和当前默认会话。
- 模型场景必须使用真实模型；预检会验证默认大脑，并确认最小聊天出现 `model.started` 和 `model.completed`。
- 所有测试数据统一使用 `CHAT-E2E-20260430-POWER` 前缀。
- 浏览器专项使用本地 `127.0.0.1` 测试站点和 `https://example.com`，外部搜索失败只记录证据，不伪造成功。

## 文件说明

- `01-测试用例-闲聊复杂知识.md`：18 条，覆盖闲聊、复杂聊天、知识总结和事实边界。
- `02-测试用例-记忆人格上下文.md`：18 条，覆盖记忆、人格、Heart 和 session 隔离。
- `03-测试用例-任务工具系统边界.md`：20 条，覆盖任务、终端、文件、审批、replay 和工具边界。
- `04-测试用例-Skill-MCP资产知识库.md`：14 条，覆盖 Skill、MCP、资产和知识库。
- `05-测试用例-浏览器执行.md`：22 条，覆盖浏览器搜索、登录、页面操作、截图、下载、异常页和安全边界。
- `06-测试用例-压力恢复安全回归.md`：16 条，覆盖 stream replay、cancel、retry、并发 session、隐私和安全回归。
- `run_chat_main_chain_power_cases.py`：自包含测试执行脚本，不 import 旧批次脚本。
- `07-重型压力测试执行报告.md`：脚本生成的执行报告。
- `08-重型压力待修复问题.md`：脚本生成的问题清单，只记录问题，不做修复。
- `09-聊天输入回复总表.md`：脚本生成的输入和回复总表，便于人工查看回复质量。

## 执行方式

在仓库根目录运行：

```powershell
.\.venv\Scripts\python.exe .\docs\测试\聊天主链路\2026-04-30\run_chat_main_chain_power_cases.py
```

## 本次执行结果

- 执行时间：2026-04-30 02:40-02:51（Asia/Shanghai）。
- 运行 ID：`20260429T184005Z`（UTC 记时）。
- 预检结果：`PASS`，`mem_xiaoyao` 默认大脑验证健康，最小真实聊天出现 `model.started` 和 `model.completed`。
- 用例结果：PASS 76 / FAIL 32 / BLOCKED 0。
- 待修复问题：46 个，其中 P0 10 个、P1 18 个、P2 18 个。
- 聊天输入回复总表：108 条用例均已落表。
- 脱敏检查：`07`、`08`、`09` 报告未命中本批次已知明文 secret/token/password/key。
- 执行期间发现多次遗留 `2026-04-29` 测试命令和 pytest 命令插队竞争当前 `data`，已作为执行环境风险记录；报告中也保留了 `database is locked` 等失败证据。

## 报告索引

- `07-重型压力测试执行报告.md`：完整输入、完整回复/结果、turn、trace、事件序列和核心证据。
- `08-重型压力待修复问题.md`：所有失败、偏差和异常按严重级别汇总。
- `09-聊天输入回复总表.md`：用于快速人工抽查聊天输入、回复摘要和判定结果。

## 开发承接

本轮测试结果用于补充 `docs/开发计划/31-第三十一阶段-真实聊天主链路全量场景缺口收敛.md` 的真实场景证据。所有失败、阻塞、设计偏差和体验问题只汇总到待修复文档，不在本轮修复。
