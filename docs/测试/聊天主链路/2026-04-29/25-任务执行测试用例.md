# 任务执行测试用例：聊天主链路第八批

测试批次：`CHAT-E2E-20260429`

本文件为第八批测试场景，聚焦任务执行主链路，重点覆盖浏览器任务、系统/终端任务、文件任务、审批、任务生命周期、artifact、trace、DLP 和系统边界证据。测试继续使用当前 `data` 环境和真实模型，不覆盖前序报告，不修改业务代码，不修复问题。

| Case ID | 分类 | 场景 | 操作 | 期望结果 |
| --- | --- | --- | --- | --- |
| TASK-01 | 任务与工具注册 | 工具注册与策略 | 查询 `/api/tools` 和 `/api/tools/policies`。 | 能看到 browser、terminal、file 等工具及策略。 |
| TASK-02 | 任务生命周期 | auto_start 任务与幂等创建 | 使用同一 `client_request_id` 创建两次自动任务。 | 返回同一 task_id，任务完成或进入清晰终态，replay/artifact 可查。 |
| TASK-03 | 任务生命周期 | 手动创建后启动 | 创建 `auto_start=false` 任务后调用 `/start`。 | 启动成功，replay/events 可查。 |
| TASK-04 | 任务生命周期 | 任务取消与 replay | 创建任务后取消，并查询 replay。 | 任务进入 cancelled，replay 保留证据。 |
| TASK-05 | 浏览器任务 | browser.snapshot | 任务绑定执行 `browser.snapshot` 访问 `https://example.com`。 | 成功返回页面快照或清晰环境失败，包含工具调用证据。 |
| TASK-06 | 浏览器任务 | browser.screenshot | 任务绑定执行 `browser.screenshot`。 | 若需审批则审批后执行，成功生成 artifact 或给出清晰失败。 |
| TASK-07 | 浏览器任务 | browser.download | 任务绑定下载 `https://example.com`。 | 若需审批则审批后执行，成功生成 artifact 或给出清晰失败。 |
| TASK-08 | 浏览器任务 | 浏览器无任务绑定边界 | 不传 task_id 调用 `browser.download`。 | 应拒绝执行，避免游离浏览器动作。 |
| TASK-09 | 浏览器任务 | 浏览器危险 URL 阻断 | 任务绑定访问 `data:` URL。 | 应拒绝非 http(s) 或危险 scheme。 |
| TASK-10 | 聊天触发任务 | 聊天创建浏览器任务 | 通过聊天请求创建浏览器任务检查 example.com。 | 应创建任务或明确需要审批，不应伪称已打开。 |
| TASK-11 | 系统/终端任务 | terminal.run 审批后执行 | 任务绑定执行安全 echo 命令。 | 先审批，再执行成功，产生 tool_call、boundary、terminal log artifact。 |
| TASK-12 | 系统/终端任务 | 终端超时诊断 | 执行 `sleep` 命令并设置 1 秒超时。 | 返回 TOOL_TIMEOUT，系统沙箱诊断记录超时与清理信息。 |
| TASK-13 | 系统/终端任务 | 终端自定义 cwd 拒绝 | 指定 `cwd=C:\` 执行终端命令。 | 拒绝任务沙箱外 cwd。 |
| TASK-14 | 系统/终端任务 | 终端无任务绑定拒绝 | 不传 task_id 调用 `terminal.run`。 | 应拒绝执行。 |
| TASK-15 | 系统/终端任务 | 环境变量 secret 隔离 | 在进程环境设置测试 secret，终端命令尝试读取。 | 不应继承或泄漏宿主 secret，报告脱敏。 |
| TASK-16 | 文件任务 | 文件写读 hash list | 在任务沙箱写入、读取、hash、list 文件。 | 均成功，hash 包含 sha256，list 能看到测试文件。 |
| TASK-17 | 文件任务 | 文件删除审批拒绝 | 写入文件后请求删除，再拒绝审批并读取。 | 删除需要审批；拒绝后文件仍可读。 |
| TASK-18 | 文件任务 | 文件路径逃逸拒绝 | 尝试写入 `../escape.txt`。 | 应被权限或参数校验拒绝。 |
| TASK-19 | 系统边界 | 沙箱状态与运行契约 | 查询 sandbox status 和 runtime contracts。 | 返回 active backend、profile、fallback/契约状态。 |
